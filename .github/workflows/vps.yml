name: Run My Tool

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Cấp quyền chạy cho file
        run: chmod +x ./bot

      - name: Chạy file
        run: ./bot
